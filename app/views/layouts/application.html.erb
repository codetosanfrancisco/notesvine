<!DOCTYPE html>
<html>
  <head>
    <title>Noteapp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all'%>
    <%= javascript_include_tag 'application'%>
    <script src="//cdn.ckeditor.com/4.10.0/standard/ckeditor.js"></script>
  </head>

  <body>
    <div class="container-fluid no-left-padding" id="<%= content_for?(:bg) ? 'bg' : 'app' %>">
      <%= render "shared/navbar"%>
      <%= render "shared/sidebar" if current_user%>
      <%= yield %>
      <%= yield :bg %>
      <%= render "shared/closing_div" if current_user%>      
    </div>
    <%= render "shared/alert"%> 
  </body>
</html>