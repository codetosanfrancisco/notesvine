<div class="row">
	<div class="col-md-8">
			<div class="page-header top-bottom-padding">
				<h1>
					<span class="font-family-banger"><%= @category.name  %></span> 
					<small>(<%= pluralize(@category.notes.size,'note')%>)</small>
				</h1>
			</div>
		</div>
</div>
<%= render "shared/search" %>
<%= render partial:"shared/got_notes",locals:{collections: @category.notes, new:"Notes"}%>
